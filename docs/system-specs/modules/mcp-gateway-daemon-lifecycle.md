# MCP gateway daemon lifecycle (one owner, one code revision)

The MCP gateway daemon (`mcp_gateway/gatewayd.py`) pools MCP backends across sessions. It is a separate process from the gateway, spawned by `mcp_gateway/manager.py::GatewayManager` with `start_new_session=True` so a gateway crash cannot take live sessions' brokers down with it. That independence is also a hazard: the daemon can outlive the gateway that spawned it, and a later gateway that finds a healthy daemon on the socket adopts it rather than spawning its own. Adoption compared what the daemon could serve (its baked target-stem map) and nothing about which code it was running. A daemon from one checkout therefore kept serving a gateway restarted from a newer one, and the pooled `kirocrew mcp-core` it handed out spoke a control-frame shape (`/api/session-directive`, `{kind, args}`) the gateway had stopped reading (`{tool, raw_args}`). Every `monitor_start` on the affected sessions was refused `not_derivable` while the tool reported success, for a day.

This module states the contract that closes it. Two gateways deliberately sharing one data home are not a supported configuration (they contend for the dashboard port, `.local_secret` and `config.json` before the daemon is ever reached), so the design is **one daemon, one owner**: the daemon belongs to the gateway process that spawned it, dies with it, and is never adopted across a code revision.

## Code fingerprint

`code_fingerprint.code_fingerprint()` is one opaque token naming WHICH Kiro Crew code a process imported, computed once per process. Git is resolved through `platform_compat.trusted_git_bin` (fixed system directories, never `PATH`) and run with global and system config disabled, inherited `GIT_*` variables dropped and `--no-ext-diff --no-textconv`, because this runs in the gateway process on a tree an agent can write to; no trusted git means the mtime rule. Inside a git worktree it is the HEAD commit plus `+<sha256 of the tracked diff>` when tracked files differ — the diff TEXT, not a dirty bit, because two uncommitted edits to one HEAD are different code, and a bare `+dirty` would let a daemon from the first edit be adopted by a gateway on the second. Otherwise it is `mtime:<digest>` over the installed distribution version and the newest `*.py` mtime under the package, since a wheel is immutable until replaced and replacing it moves the mtimes. The only contract is *equal iff the same code*. The first computation runs `git` or walks the package tree, so the two async owners — `run_gatewayd` before it binds, `GatewayManager._start_locked` before its election loop — await `warm_code_fingerprint()` (`asyncio.to_thread`) once; every later synchronous read on their event loops (the pong, the stand-down handler, `_code_drift`) is a cache hit. The stub computes it inside `build_register_payload`, which already runs on the subprocess executor.

This exists because nothing else could answer the question. The MCP stub's `binary_version` (`mcp_gateway/stub.py::_binary_version`) hashes the target binary, and for Kiro Crew's own servers that binary is the `.venv/bin/kirocrew` console-script shim — byte-identical across every commit of an editable install. The distribution version changes only at a release.

## Owner liveness

`GatewayManager._spawn_once` passes `--owner-pid <gateway pid>` on the daemon's argv. `run_gatewayd` publishes it (`_OWNER_PID`) and, after a successful bind, arms `_owner_liveness_sweeper`: every `_OWNER_LIVENESS_INTERVAL_SECS` (15 s) it checks that the owner PID exists AND that its `platform_compat.process_start_time` equals the value read at arm time, so a recycled PID number is not mistaken for the owner. `_OWNER_LIVENESS_MISSES` (2) consecutive conclusive misses set `stop_event`, which is the same graceful path SIGTERM takes: accepts stop, attached stubs drain, `pool.shutdown_all()` takes every pooled backend down, the endpoint and the singleton lock are released. An unreadable start time at arm time disables the check with a warning (nothing to compare against); an unreadable probe later is inconclusive and neither counts nor resets. No `--owner-pid` (an operator running the module by hand) arms nothing.

The same rule reaches the CLI. `cli_server._stop` — and therefore `kirocrew restart`, which reuses it — calls `_stop_mcp_gateway_daemon` after signalling the gateway, on both the service and the listener path. That goes through `mcp_gateway/daemon_control.py::stop_daemon`: ping the configured socket (`configured_socket_path`: `mcp_gateway.socket_path` when set, the data-home default otherwise — the same resolution the gateway uses to spawn), read the daemon's `pid` from the pong, verify that process's command line names `kiro_crew.mcp_gateway.gatewayd` (a forged pong cannot aim a signal), SIGTERM it through `platform_compat.kill_pid_pinned` on the `start_time` the daemon reported about itself in the pong — computed once at daemon startup off the loop (`_OWN_START_TIME`), since on macOS it is a `ps` subprocess and the pong is answered from the connection handler — re-confirmed against `process_start_time(pid)` immediately before the signal on POSIX, where `kill_pid_pinned` has no handle to hold (a daemon that reports no token is refused), so a PID recycled between the ping and the signal is not signalled — never SIGKILL, because the drain is what reaps the pool — and wait up to the daemon's published shutdown budget for the singleton lock to free. The synchronous stop matters even though the daemon would exit on its own: a restart spawns the replacement gateway inside the owner-probe interval, and that gateway would otherwise meet a healthy daemon and adopt it. Every outcome (`stopped`, `draining`, `denied`, `unverified`, `absent`) is reported and none fails the command.

## The adoption gate

The daemon's ping reply (`gatewayd._pong_payload`) carries `targets`, `fingerprint`, `owner_pid` and `pid`.

A fast ping is bounded at `_PING_TIMEOUT_SECS`, which under load measures how loaded the daemon is rather than whether it is alive -- and because `_ping_raw` gives up at that bound, a late pong is never seen at all. So every decision that would DISPLACE a running daemon escalates first (`_ping_with_escalation`): the fast probe, then one further probe at `_LIVENESS_ESCALATED_TIMEOUT_SECS` if it missed. A daemon that answers either is alive, and the owned path's `_LIVENESS_MAX_CONSECUTIVE_FAILURES` grace counts only cycles where neither answered. The adopted path escalates too, and then acts on ONE miss rather than carrying the owned path's `_LIVENESS_MAX_CONSECUTIVE_FAILURES` grace. That asymmetry is deliberate: on the adopted path the verdict's latency IS the outage. An adopted daemon that really is gone leaves nothing listening on its address, so no attached stub can serve a call until a replacement binds. One escalated miss displaces at 26s worst case -- 2+2+2 for the fast probe's three steps, then 20 for the escalated one -- while three cycles would take 26 + 30 + 26 + 30 + 26 = 138s, five times the outage in exchange for evidence the escalation already provides. The load misverdict the escalation exists for is already handled by the escalation itself: a daemon that answers either probe is alive and is never displaced, and no case has been observed of a live daemon silent past the escalated bound. The owned path can afford its grace because its own socket stays bound while it waits. The one-shot assessment gates (the start-time election, the post-respawn incumbent check) and the public `GatewayManager.ping()` status probe keep the fast bound. For the status probe that is right: a readout must answer a UI poll promptly, and a wrong readout displaces nothing. For the assessment gates it is a SCOPE boundary, not a safety argument. Their miss path reaches `_clear_stale_socket` -> `transport.remove_stale`, which asks `transport.probe_live` whether the endpoint is live before unlinking it -- and on POSIX that probe reads a full accept backlog as not-live, because a unix `connect()` against a saturated listener blocks until the probe's own 1s timeout and the resulting `socket.timeout` is an `OSError`, which the probe reports as False. So under the very load this escalation exists for, an assessment gate can unlink a LIVE daemon's socket and block every new and reconnecting stub. The root of that is `probe_live` treating a timeout as a negative answer while its own contract says an inconclusive probe must err toward leaving the endpoint alone; fixing it belongs with the endpoint lifecycle, not with the supervisor's verdict, and is not attempted here.

A stub whose live connection dies mid-session retries the Register handshake within `stub._RECONNECT_TOTAL_BUDGET_SECS`, and that budget has to outlast the recovery it is waiting for. The owned liveness path cannot be faster than 91s -- three `_LIVENESS_PING_INTERVAL_SECS` cycles must elapse before the third failure can be counted at all -- and runs to about 191s once the probes and the SIGTERM wait are added, so a budget below that is not patience but a guarantee that an ORDINARY slow recovery costs every attached session its MCP tools for good. The budget therefore clears the derived worst case several times over rather than being tuned to a typical restart, and a test asserts it against the manager's own constants so raising the detection cost fails there instead of silently re-breaking this. It is deliberately not a cover for every conceivable recovery: a respawn that keeps failing backs off to 60s per retry with no attempt cap, and past five minutes the honest signal to a waiting session is that its tools are gone rather than more silence. Requests already in flight when the connection drops are failed fast with `-32603` before any retry begins. A request kiro-cli issues DURING the window is not: the bridge pumps are down, so it waits in the stub's stdin queue until the reattach or the budget ends, which is why the budget is sized to cover the recovery and no more rather than generously. That gap is tracked separately. A refusal -- an older daemon that cannot honour `poolable`, or a generation that answers the handshake differently -- stays terminal rather than being retried.

`GatewayManager` reads the incumbent's pong at every point it might adopt — the start-time election, the lost-the-lock branch, the watchdog's periodic re-check of an adopted daemon (`_reconcile_adopted`), and the watchdog's re-election after a daemon dies — and adopts only when `_adoption_drift` (target stems), `_code_drift` (fingerprint) AND `_owned_by_a_live_other` are all clear. The last one reads the pong's `owner_pid`: an owner that is alive and is not this process means another gateway still owns that daemon. That is either the unsupported two-gateways-one-home layout or a restart racing the old gateway's exit, and in both cases adopting would take a daemon whose owner sweeper is about to shut it down mid-traffic. The start refuses the broker (`ERROR`, returns False; stubs fall back to per-session exec) and the watchdog backs off and looks again, rather than asking that daemon to stand down — it is not ours to cycle. An owner that is absent (0) or this process is not a rival, and the other two gates decide. An owner that is DEAD makes the daemon an orphan (`_orphaned`), and an orphan is not adopted either, even on matching stems and code: its own sweeper ends it within two probes, so adopting it hands every session a broker that exits under them mid-call. It is treated as draining — asked to stand down on the `orphaned` ground, which the daemon honours only after confirming from its own `_OWNER_PID` that the owner really is gone (a caller's claim ends no daemon whose gateway is alive) — and replaced by a daemon this process owns. `_code_drift` is true when the fingerprint differs from this process's own OR is absent: a daemon that omits it predates this code by construction and is stale, not unverifiable.

Either drift asks the incumbent to stand down through `_request_stand_down`, whose frame now also carries `caller_fingerprint` when code drift is the ground. `gatewayd._apply_stand_down` honours a request on two independent grounds — a needed stem it cannot resolve, or a caller fingerprint that differs from its own — and still refuses one it has no reason to honour: a daemon that resolves every requested stem AND matches the caller's code has nothing to gain from cycling. The reply names both grounds (`missing`, `stale_code`) and the SEL audit row records them. An old-shape frame (stems only) is handled exactly as before.

The fail-open branch is unchanged in kind and louder in degree: a stale daemon that REFUSES is still serving, so it is adopted rather than left unsupervised, but for code drift the log line is an ERROR naming the consequence (pooled backends may speak a stale protocol) and the fix (`kirocrew restart`).

## The pool key

`stub.pool_binary_version` folds `code_fingerprint()` into the registered `binary_version` when the target args name one of Kiro Crew's own servers (`_KIROCREW_MCP_SUBCOMMANDS`: `mcp-core`, `mcp-cron`, `mcp-work`, `mcp-computer`, `mcp-dashboard`) and leaves third-party servers on the binary hash alone. So even a daemon that survives — an operator-run one with no owner, or one whose stand-down was refused — hands a stub from new code a NEW pool partition rather than the old checkout's backend; a `git pull` cold-starts Kiro Crew's own servers and nothing else.

## Diagnosis

`dashboard/handlers/sessions.py::api_session_directive` recognises the pre-call-input body (`kind` present, `tool` absent) and refuses it as `stale_mcp_backend`, with a warning that names the cause (an MCP server running older code than the gateway) and the fix, instead of the generic `not_derivable` that pointed operators at the directive tools. `kirocrew doctor` (`cli_doctor._doctor_mcp_gateway_daemon`) prints the daemon's pid, owner and fingerprint beside this install's, and records an issue when they differ; the same read is `daemon_control.describe_daemon`.

## Overload is not death

The watchdog (`GatewayManager._run_watchdog`) supervises the daemon it owns by two signals: process exit, and a periodic ping. `_liveness_probe_loop` pings every `_LIVENESS_PING_INTERVAL_SECS` (30 s) and, after `_LIVENESS_MAX_CONSECUTIVE_FAILURES` (3) consecutive misses, returns a zombie verdict that the race turns into SIGTERM→SIGKILL and a respawn. The ping catches the silent zombie (accept loop dead, Python process alive) that `proc.wait()` cannot. It cannot, on its own, tell that zombie from a daemon whose event loop is merely saturated: under a ~1000-session fan-out the daemon has carried 100-185 connections in flight, a short ping queued behind them times out three times running, and the watchdog killed a daemon that was serving the whole time — ten times in thirty-five minutes, each kill severing every attached session's kirocrew-core transport and cold-starting every pooled backend. The two conditions call for opposite actions, so a cycle counts as a miss only when the daemon answered NEITHER probe.

**One escalated probe is what separates them.** Each cycle asks twice: the fast ping, bounded per step at `_PING_TIMEOUT_SECS` (2 s) — the bound the ordinary call sites keep, and the bound `_ping_raw` gives up at, so a late pong is otherwise never even seen — and then, only if that missed, ONE further probe at `_LIVENESS_ESCALATED_TIMEOUT_SECS` (20 s) as a SHARED whole-round-trip bound rather than 20 s per step. A daemon that answers either probe is alive and the run of misses resets; only a cycle in which neither answered counts toward `_LIVENESS_MAX_CONSECUTIVE_FAILURES`, and there is no further reprieve past the third. So a verdict costs at worst 3 × (30 s interval + 6 s fast probe + 20 s escalated probe) = 168 s, cannot arrive faster than the three intervals (90 s), and `_SHUTDOWN_GRACE_SECS` (20 s) more elapses before the SIGKILL. The full argument for the escalation — why it must reach every step but not multiply per step, the deliberate one-miss asymmetry on the adopted path, and which call sites keep the fast bound and why — is [§The adoption gate](#the-adoption-gate).

## Respawn backoff is the ladder's L4 rung

The watchdog's respawn delay is read from the shared recovery ladder, not held
here: `_RESPAWN_BACKOFF_START_SECS` / `_RESPAWN_BACKOFF_MAX_SECS` are
`recovery.ladder.LADDER.layer(L4_GATEWAYD).base_secs / max_secs` (1s / 60s), and
every doubling goes through `GatewayManager._next_respawn_backoff`, which doubles,
caps and applies equal jitter (`[raw/2, raw]`) so two supervisors that lost their
daemons together do not respawn in lock-step. The two names survive because the
stub mirrors the cap by name (`_SUPERVISOR_RESPAWN_BACKOFF_MAX_SECS`) and
`test_stub_reconnect_budget.py` pins the mirror; L4 is `pinned` in the ladder
for the same reason -- the stub's 600s reconnect budget is three kill->respawn
cycles that each include this cap, so the `agent.recovery_backoff_*` knobs
never move it. After each successful respawn the watchdog records
`kirocrew.recovery.restarts{layer=L4_gatewayd}` and one L4 failure on the
ladder; a SECOND respawn inside the 600s window escalates to L5, which is one
notification -- the loop keeps supervising, and an automatic gateway restart is
out of scope. Thirty seconds alive resets both the local backoff and the
ladder's count. See [session.md § Recovery ladder](session.md#recovery-ladder-recoverypolicypy-recoveryladderpy).

## Overload is admitted, not forked

A daemon that stays alive under a fan-out still has to decide what to do with a thousand `ensure_backend` frames arriving together. Its answer used to be "fork": pooled keys were deduplicated per key and capped as a RESIDENT count by `max_backends`, private backends were bounded by nothing, and both paths refused-or-fell-back rather than waited -- so a burst turned into hundreds of concurrent `create_subprocess_exec` calls on one loop, each reading the interpreter tree off one saturated disk, and every refusal spawned a per-session exec that made the disk busier still. The contract now is **admission before allocation**: `mcp_gateway/host_budget.py::HostBudget` (every backend the daemon is answerable for -- pooled, private, and the fallback exec a rejection causes -- is charged before its fork and released when it is reaped, never when `shutdown` returns), `mcp_gateway/admission.py::SpawnGate` (one daemon-wide count of spawn+initialize windows in flight, FIFO past it, fixed capacity behind a `set_capacity` seam, permits whose exactly-once `settle` is separate from their idempotent `release`, a detached initialize watcher so `ready` is never delayed and an unused prewarm is never read as congestion) and `BackendPool.reserve_resident_slot` (the `max_backends` check moved in front of the fork). The order inside `_acquire_backend`'s spawn closure is permit → budget → slot → fork, released in reverse; the only step that waits is the gate, and a gate waiter holds nothing another waiter needs, so the wait-for graph is acyclic. The gate is FIRST for that clause: a charge taken before the wait prices a process that does not exist for as long as the wait lasts (up to `spawn_queue_wait_secs`, 600 s), so a deep enough queue exhausts the ceiling with nothing running and refuses the next stub `capacity` -- which authorises no fallback -- on an idle host. The daemon's drain closes admission first. A connection that keeps writing while its acquire waits is bounded too: `_await_answering_pings` parks every non-ping frame in one per-connection deque that ONLY the main loop drains, and the main loop cannot run until the wait returns, so it counts both frames and bytes against `_MAX_PENDING_FRAMES` / `_MAX_PENDING_BYTES` and past either bound cancels the pending acquire and drops that ONE connection, leaving co-pooled sessions untouched. Same guard class as `backend._STUB_INBOX_MAXSIZE` in the reverse direction. Both dimensions are load-bearing (a count bound alone admits that many maximum-size frames; a byte bound alone leaves the per-object overhead of millions of tiny frames unaccounted), the byte bound follows `read_buffer_limit_bytes` upward so one frame the reader was willing to return can never trip it alone and is floored at that key's shipped default so tuning it DOWN cannot tighten the park, and the overflowing frame is parked before the bound is read so no frame is ever dropped while the peer is told nothing. The protocol half -- `spawn_queue` in `REGISTERED_CAPABILITIES`, `wait_budget_secs` on `ensure_backend`, `queued` keepalives, classed `rejected` frames (`capacity` / `compat` / `isolation`), pings answered while an acquire waits, the stub's silence timer and its typed `-32001` capacity error -- is specified in [`docs/architecture/mcp.md`](../../architecture/mcp.md#admission-before-allocation), and the config keys (`spawn_concurrency_initial/min/max`, `spawn_queue_wait_secs`, `initialize_timeout_secs`, `host_budget_max_procs/rss_mb/fds`, all `restart=True`, all on the daemon's argv from `GatewayManager._spawn_once`, which also samples available memory for the `0 = auto` ceilings) in [`config.md`](config.md).

## The gate's capacity is moved, not configured

`spawn_concurrency_initial` is where the gate STARTS; the live capacity is owned by the gateway's adaptive concurrency controller ([`adaptive-concurrency.md`](adaptive-concurrency.md)). It moves it through one more one-shot control frame on the uid-gated socket, beside `claim` / `abort` / `stand-down`: `{"type": "set-spawn-capacity", "capacity": n}` (sent by `GatewayManager.set_spawn_capacity`, over `_control_roundtrip`) is answered by `gatewayd._apply_set_spawn_capacity` with `{"type": "spawn-capacity", "capacity": <what took effect>, ...gate snapshot}` after `admission.gate.set_capacity(n)`, or `spawn-capacity-rejected` for a frame with no usable integer (a non-number, a boolean, `NaN`, and `±inf` -- which `json.loads` produces from `1e400` and `int()` refuses) or a daemon with no admission. Every value earns one of those two answers, because an exception raised here escapes to the connection handler, which drops the control connection with no reply at all: the controller would then keep retrying a value against a daemon that never answers. The gate clamps to `[spawn_concurrency_min, spawn_concurrency_max]` and never revokes an in-flight spawn, so the reply reports the clamped value and the controller keeps a value the daemon did not confirm PENDING and retries it on its next tick. The verdict that moves it is the host's: corroborated pressure halves the capacity (to no lower than the floor), a clean window earns `+1` when the gate's own `success` outcomes and its queue say there is demand, severe pressure holds it at the floor while the gateway pauses its own dispatch. A daemon that never receives the frame runs at its configured initial forever -- the fixed-capacity contract above is the floor the controller builds on, not something it replaces.

## Tests

`test_adaptive_controller.py::TestGatewaydFrame`: the frame moves the gate and reports the clamped value; a non-integer, a boolean and a daemon without admission are rejected; `GatewayManager.set_spawn_capacity` returns the applied value on `spawn-capacity` and `None` on no reply or a rejection.

`test_mcp_gateway_spawn_gate.py`: the gate admits in arrival order and a newcomer never jumps the queue; `settle` is exactly-once and `release` idempotent, settling neutral when nothing was recorded; a cancelled waiter leaves the queue counting nothing and a cancel landing after the grant hands the slot back; raising capacity admits waiters and cutting it revokes nothing; the keepalive callback reports one-based positions and the deadline raises `SpawnGateTimeout`; `close` fails waiters, refuses newcomers and releases watchers; the initialize watcher settles success on ready, failure-and-hold-until-reaped on a failed handshake, neutral on no handshake, and grants one more window to a handshake still in flight; a host charge follows the process and `Admission.close` drops every charge. On the wire, through `_handle_connection` with a scripted reader: no `capacity` refusal carries `fallback` on EITHER wire shape -- pinned per capacity failure, every pressure errno included and the errno table held equal to `_PRESSURE_ERRNOS`, and pinned again for eight simultaneous legacy refusals against a budget of one, because the leak that tag authorised was a RATE (a pre-`spawn_queue` stub closes its socket before exec'ing, so the daemon's charge is released before the process exists); a target-shaped `compat` refusal on the same legacy wire still carries it and is still charged until the stub hangs up, which is what keeps this from being a blanket refusal; an old stub never sees `queued`; a new stub gets a classed rejection with `retry_after_secs`; a queued new stub receives `queued` keepalives and its pings are answered while a non-control frame is parked for after `ready`; a burst one frame under `_MAX_PENDING_FRAMES` is parked whole and in arrival order while either bound exceeded -- count or bytes, each moved rather than met so no test allocates a 64 MiB frame -- cancels the acquire and drops that connection with no `ready` and no `rejected`, the pings answered along the way never counting toward the bound; a fallback is charged to the budget until the stub hangs up and becomes `capacity` when the budget cannot take it; `stats` carries the admission snapshot; and against a real pool, the budget is released with a gate failure, resident capacity is refused before any fork, prewarm settles neutral at once while a stub spawn holds its permit through initialize. Three of its pins are timing rather than shape and are measured end to end for that reason: with a real endpoint, the real `stub._ensure_backend_admitted` and the real handler on either side of it and ONE budget used by both -- the shipped shape -- the stub receives the `capacity` refusal and not its own timeout, because a timeout is what makes it exec; `_negotiated_wait_budget` is strictly under what the stub asked for at every budget including the shipped 600/600 pair; two spawns queued at the gate hold ZERO host charges, so a third meets the queue rather than a full host; a non-finite `set-spawn-capacity` earns the typed refusal and leaves the live capacity where it was; and a real `run_gatewayd` prewarm pass whose first key is warmed while a stub queues stands the SECOND key down and takes a bounded `wait_deadline` (both are arguments the pass passes in, so `_acquire_backend` cannot be where they are pinned). `test_mcp_gateway_host_budget.py`: reserve/release, per-dimension exhaustion leaving counters untouched, `release_all`, and the auto-ceiling rules (never below the pool; floors without a memory sample; a share of the descriptor soft limit). `test_mcp_gateway_exclusive_backend.py` (resident slots): refused before anything is spawned, pending slots count until `add` consumes them and the same key reuses its slot, release frees the unit, a key with a live entry gets a no-op slot, an idle entry is LRU-evicted as `add` did, `get_or_create` reaps a slot its spawn left behind, private backends take none. `test_mcp_gateway_pool_integ.py` (real processes, real endpoint): a storm of private stubs against a slow-init fake all reach `ready`, no more than the gate's capacity of spawn+initialize windows are ever open at once (read from the fake's launch/initialized stamps) and no fallback record is written. `test_mcp_gateway_stub_admission_fallback.py`: `queued` frames extend a queue-aware wait while silence times it out before the budget and the budget caps it however lively the daemon; an old daemon gets the budget-less frame and its single reply is the verdict, a `queued` frame included; only `compat`/`isolation` fall back; a capacity refusal answers every request with the typed error and drops notifications.

`test_mcp_gateway_liveness_verdict.py`: a loaded daemon that answers only the escalated probe is never counted a liveness failure; a daemon that answers neither probe still trips at exactly `_LIVENESS_MAX_CONSECUTIVE_FAILURES`; the adopted path escalates too, so a loaded adopted daemon is not read as gone while an absent one still is, and one escalated miss replaces an adopted daemon with no second probe and no wait, which pins the deliberate asymmetry; and the escalated timeout bounds every step of the probe AND caps their total, because per step the escalated probe alone would reach 3 x 20s and make the probe the largest term in an outage it is supposed to end, while the default path keeps the fast bound on each step exactly as on base. `test_mcp_gateway_daemon_lifecycle.py`: the fingerprint's git rule including two uncommitted edits on one HEAD differing, the mtime rule, and stability; the owner sweeper on a dead owner, a recycled PID, an inconclusive probe and a missing baseline; a REAL daemon spawned against a throwaway owner process exiting rc=0 when that owner is killed; `_code_drift` on matching, different and absent fingerprints; `_owned_by_a_live_other` and `_orphaned` on absent, zero, junk, self, dead and live owners; a stems-fit, code-matching daemon owned by a live other gateway refused (no stand-down, no spawn, ERROR logged) and the same daemon with a dead owner asked to stand down with `orphaned=True` and replaced; the daemon honouring an orphan claim only when its own recorded owner is gone and refusing it for a live owner or no owner; warming computing off the main thread and filling the cache; a fit-by-stems but stale-by-code incumbent being asked to stand down with `stale_code=True`; the stand-down frame carrying the caller fingerprint; the daemon honouring a differing caller fingerprint, refusing a matching one, and still honouring the old stems-only frame; the pong's fields; the pool-key fold for Kiro Crew servers only. `test_mcp_gateway_daemon_control.py`: `describe_daemon`/`stop_daemon` on every outcome including the refusal to signal a pid that is not gatewayd, the CLI reporting each outcome without failing the stop on both the service and listener paths, and the doctor line for same, different, pre-fingerprint and absent daemons. `test_directive_queue.py` pins the `stale_mcp_backend` refusal and its wording. `test_mcp_gateway_adopted_daemon_repair.py`'s fit pongs now carry the real fingerprint, so the target-drift behaviour it pins is unchanged.
